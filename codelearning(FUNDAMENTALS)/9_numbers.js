const score=100
// console.log(score)//Javascript automatically optimises the values datatype here it is Number
const balance=new Number(100) //this is  defining that it is a number
// console.log(balance)
// console.log(balance.toString())//converting the type to string
// console.log(balance.toString().length)//now we can use all string methods
 const otherNumber = 123.8966
//  console.log(otherNumber.toPrecision(4))//this rounds off the value nd keep it to 4
const hundreds=100000
console.log(hundreds.toLocaleString('en-IN'))//it adds comas in the form of indian number system

